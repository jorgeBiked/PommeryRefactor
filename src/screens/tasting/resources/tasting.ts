import { language } from '../../../utilities/Constants';
import * as data from '../../../localisations/tastingBootles.json';
import { createBootle } from '../../common/content';

export const bootle1 = createBootle((<any>data)[language].bootle1)
export const bootle2 = createBootle((<any>data)[language].bootle2)
export const bootle3 = createBootle((<any>data)[language].bootle3)
export const bootle4 = createBootle((<any>data)[language].bootle4)
export const bootle5 = createBootle((<any>data)[language].bootle5)